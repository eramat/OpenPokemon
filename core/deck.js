var OpenPokemon = OpenPokemon || {};

module.exports = ( function (self) {
  "use strict";

  self.Deck = function () {
    var pokemon_cards;
    var energy_cards;
    var trainer_cards;

    var init = function () {
    };

    init();
  };

  return self;
}(OpenPokemon || {}));